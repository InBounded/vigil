/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type Address,
  combineCodec,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/program-client-core";
import { SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS } from "../programs/index.js";

export const VAULT_TRANSACTION_ACCOUNTS_CLOSE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  196, 71, 187, 176, 2, 35, 170, 165,
]);

export function getVaultTransactionAccountsCloseDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    VAULT_TRANSACTION_ACCOUNTS_CLOSE_DISCRIMINATOR,
  );
}

export type VaultTransactionAccountsCloseInstruction<
  TProgram extends string = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
  TAccountMultisig extends string | AccountMeta<string> = string,
  TAccountProposal extends string | AccountMeta<string> = string,
  TAccountTransaction extends string | AccountMeta<string> = string,
  TAccountRentCollector extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMultisig extends string ? ReadonlyAccount<TAccountMultisig> : TAccountMultisig,
      TAccountProposal extends string ? WritableAccount<TAccountProposal> : TAccountProposal,
      TAccountTransaction extends string
        ? WritableAccount<TAccountTransaction>
        : TAccountTransaction,
      TAccountRentCollector extends string
        ? WritableAccount<TAccountRentCollector>
        : TAccountRentCollector,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type VaultTransactionAccountsCloseInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type VaultTransactionAccountsCloseInstructionDataArgs = {};

export function getVaultTransactionAccountsCloseInstructionDataEncoder(): FixedSizeEncoder<VaultTransactionAccountsCloseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: VAULT_TRANSACTION_ACCOUNTS_CLOSE_DISCRIMINATOR,
    }),
  );
}

export function getVaultTransactionAccountsCloseInstructionDataDecoder(): FixedSizeDecoder<VaultTransactionAccountsCloseInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getVaultTransactionAccountsCloseInstructionDataCodec(): FixedSizeCodec<
  VaultTransactionAccountsCloseInstructionDataArgs,
  VaultTransactionAccountsCloseInstructionData
> {
  return combineCodec(
    getVaultTransactionAccountsCloseInstructionDataEncoder(),
    getVaultTransactionAccountsCloseInstructionDataDecoder(),
  );
}

export type VaultTransactionAccountsCloseInput<
  TAccountMultisig extends InstructionAccountInput = InstructionAccountInput,
  TAccountProposal extends InstructionAccountInput = InstructionAccountInput,
  TAccountTransaction extends InstructionAccountInput = InstructionAccountInput,
  TAccountRentCollector extends InstructionAccountInput = InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput = InstructionAccountInput,
> = {
  multisig: TAccountMultisig;
  /** the logic within `vault_transaction_accounts_close` does the rest of the checks. */
  proposal: TAccountProposal;
  /** VaultTransaction corresponding to the `proposal`. */
  transaction: TAccountTransaction;
  /** The rent collector. */
  rentCollector: TAccountRentCollector;
  systemProgram?: TAccountSystemProgram;
};

export function getVaultTransactionAccountsCloseInstruction<
  TAccountMultisig extends InstructionAccountInput,
  TAccountProposal extends InstructionAccountInput,
  TAccountTransaction extends InstructionAccountInput,
  TAccountRentCollector extends InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
>(
  input: VaultTransactionAccountsCloseInput<
    TAccountMultisig,
    TAccountProposal,
    TAccountTransaction,
    TAccountRentCollector,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): VaultTransactionAccountsCloseInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountMultisig,
    InstructionAccountInputAddress<TAccountMultisig>
  >,
  ResolvedInstructionAccountMeta<
    TAccountProposal,
    InstructionAccountInputAddress<TAccountProposal>
  >,
  ResolvedInstructionAccountMeta<
    TAccountTransaction,
    InstructionAccountInputAddress<TAccountTransaction>
  >,
  ResolvedInstructionAccountMeta<
    TAccountRentCollector,
    InstructionAccountInputAddress<TAccountRentCollector>
  >,
  ResolvedInstructionAccountMeta<
    TAccountSystemProgram,
    InstructionAccountInputAddress<TAccountSystemProgram>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    multisig: {
      value: input.multisig ?? null,
      isSigner: false,
      isWritable: false,
    },
    proposal: {
      value: input.proposal ?? null,
      isSigner: false,
      isWritable: true,
    },
    transaction: {
      value: input.transaction ?? null,
      isSigner: false,
      isWritable: true,
    },
    rentCollector: {
      value: input.rentCollector ?? null,
      isSigner: false,
      isWritable: true,
    },
    systemProgram: {
      value: input.systemProgram ?? null,
      isSigner: false,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("multisig", accounts.multisig),
      getAccountMeta("proposal", accounts.proposal),
      getAccountMeta("transaction", accounts.transaction),
      getAccountMeta("rentCollector", accounts.rentCollector),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getVaultTransactionAccountsCloseInstructionDataEncoder().encode({}),
    programAddress,
  } as VaultTransactionAccountsCloseInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountMultisig,
      InstructionAccountInputAddress<TAccountMultisig>
    >,
    ResolvedInstructionAccountMeta<
      TAccountProposal,
      InstructionAccountInputAddress<TAccountProposal>
    >,
    ResolvedInstructionAccountMeta<
      TAccountTransaction,
      InstructionAccountInputAddress<TAccountTransaction>
    >,
    ResolvedInstructionAccountMeta<
      TAccountRentCollector,
      InstructionAccountInputAddress<TAccountRentCollector>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type ParsedVaultTransactionAccountsCloseInstruction<
  TProgram extends string = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    multisig: TAccountMetas[0];
    /** the logic within `vault_transaction_accounts_close` does the rest of the checks. */
    proposal: TAccountMetas[1];
    /** VaultTransaction corresponding to the `proposal`. */
    transaction: TAccountMetas[2];
    /** The rent collector. */
    rentCollector: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: VaultTransactionAccountsCloseInstructionData;
};

export function parseVaultTransactionAccountsCloseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedVaultTransactionAccountsCloseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      multisig: getNextAccount(),
      proposal: getNextAccount(),
      transaction: getNextAccount(),
      rentCollector: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getVaultTransactionAccountsCloseInstructionDataDecoder().decode(instruction.data),
  };
}
