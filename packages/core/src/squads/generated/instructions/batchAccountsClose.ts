/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type Address,
  combineCodec,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlyUint8Array,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/program-client-core";
import { SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS } from "../programs/index.js";

export const BATCH_ACCOUNTS_CLOSE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  218, 196, 7, 175, 130, 102, 11, 255,
]);

export function getBatchAccountsCloseDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BATCH_ACCOUNTS_CLOSE_DISCRIMINATOR);
}

export type BatchAccountsCloseInstruction<
  TProgram extends string = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
  TAccountMultisig extends string | AccountMeta<string> = string,
  TAccountProposal extends string | AccountMeta<string> = string,
  TAccountBatch extends string | AccountMeta<string> = string,
  TAccountRentCollector extends string | AccountMeta<string> = string,
  TAccountSystemProgram extends string | AccountMeta<string> = "11111111111111111111111111111111",
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMultisig extends string ? ReadonlyAccount<TAccountMultisig> : TAccountMultisig,
      TAccountProposal extends string ? WritableAccount<TAccountProposal> : TAccountProposal,
      TAccountBatch extends string ? WritableAccount<TAccountBatch> : TAccountBatch,
      TAccountRentCollector extends string
        ? WritableAccount<TAccountRentCollector>
        : TAccountRentCollector,
      TAccountSystemProgram extends string
        ? ReadonlyAccount<TAccountSystemProgram>
        : TAccountSystemProgram,
      ...TRemainingAccounts,
    ]
  >;

export type BatchAccountsCloseInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type BatchAccountsCloseInstructionDataArgs = {};

export function getBatchAccountsCloseInstructionDataEncoder(): FixedSizeEncoder<BatchAccountsCloseInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: BATCH_ACCOUNTS_CLOSE_DISCRIMINATOR,
    }),
  );
}

export function getBatchAccountsCloseInstructionDataDecoder(): FixedSizeDecoder<BatchAccountsCloseInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getBatchAccountsCloseInstructionDataCodec(): FixedSizeCodec<
  BatchAccountsCloseInstructionDataArgs,
  BatchAccountsCloseInstructionData
> {
  return combineCodec(
    getBatchAccountsCloseInstructionDataEncoder(),
    getBatchAccountsCloseInstructionDataDecoder(),
  );
}

export type BatchAccountsCloseInput<
  TAccountMultisig extends InstructionAccountInput = InstructionAccountInput,
  TAccountProposal extends InstructionAccountInput = InstructionAccountInput,
  TAccountBatch extends InstructionAccountInput = InstructionAccountInput,
  TAccountRentCollector extends InstructionAccountInput = InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput = InstructionAccountInput,
> = {
  multisig: TAccountMultisig;
  /** the logic within `batch_accounts_close` does the rest of the checks. */
  proposal: TAccountProposal;
  /** `Batch` corresponding to the `proposal`. */
  batch: TAccountBatch;
  /** The rent collector. */
  rentCollector: TAccountRentCollector;
  systemProgram?: TAccountSystemProgram;
};

export function getBatchAccountsCloseInstruction<
  TAccountMultisig extends InstructionAccountInput,
  TAccountProposal extends InstructionAccountInput,
  TAccountBatch extends InstructionAccountInput,
  TAccountRentCollector extends InstructionAccountInput,
  TAccountSystemProgram extends InstructionAccountInput,
  TProgramAddress extends Address = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
>(
  input: BatchAccountsCloseInput<
    TAccountMultisig,
    TAccountProposal,
    TAccountBatch,
    TAccountRentCollector,
    TAccountSystemProgram
  >,
  config?: { programAddress?: TProgramAddress },
): BatchAccountsCloseInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountMultisig,
    InstructionAccountInputAddress<TAccountMultisig>
  >,
  ResolvedInstructionAccountMeta<
    TAccountProposal,
    InstructionAccountInputAddress<TAccountProposal>
  >,
  ResolvedInstructionAccountMeta<TAccountBatch, InstructionAccountInputAddress<TAccountBatch>>,
  ResolvedInstructionAccountMeta<
    TAccountRentCollector,
    InstructionAccountInputAddress<TAccountRentCollector>
  >,
  ResolvedInstructionAccountMeta<
    TAccountSystemProgram,
    InstructionAccountInputAddress<TAccountSystemProgram>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    multisig: {
      value: input.multisig ?? null,
      isSigner: false,
      isWritable: false,
    },
    proposal: {
      value: input.proposal ?? null,
      isSigner: false,
      isWritable: true,
    },
    batch: { value: input.batch ?? null, isSigner: false, isWritable: true },
    rentCollector: {
      value: input.rentCollector ?? null,
      isSigner: false,
      isWritable: true,
    },
    systemProgram: {
      value: input.systemProgram ?? null,
      isSigner: false,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Resolve default values.
  if (!accounts.systemProgram.value) {
    accounts.systemProgram.value =
      "11111111111111111111111111111111" as Address<"11111111111111111111111111111111">;
  }

  return Object.freeze({
    accounts: [
      getAccountMeta("multisig", accounts.multisig),
      getAccountMeta("proposal", accounts.proposal),
      getAccountMeta("batch", accounts.batch),
      getAccountMeta("rentCollector", accounts.rentCollector),
      getAccountMeta("systemProgram", accounts.systemProgram),
    ],
    data: getBatchAccountsCloseInstructionDataEncoder().encode({}),
    programAddress,
  } as BatchAccountsCloseInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountMultisig,
      InstructionAccountInputAddress<TAccountMultisig>
    >,
    ResolvedInstructionAccountMeta<
      TAccountProposal,
      InstructionAccountInputAddress<TAccountProposal>
    >,
    ResolvedInstructionAccountMeta<TAccountBatch, InstructionAccountInputAddress<TAccountBatch>>,
    ResolvedInstructionAccountMeta<
      TAccountRentCollector,
      InstructionAccountInputAddress<TAccountRentCollector>
    >,
    ResolvedInstructionAccountMeta<
      TAccountSystemProgram,
      InstructionAccountInputAddress<TAccountSystemProgram>
    >
  >);
}

export type ParsedBatchAccountsCloseInstruction<
  TProgram extends string = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    multisig: TAccountMetas[0];
    /** the logic within `batch_accounts_close` does the rest of the checks. */
    proposal: TAccountMetas[1];
    /** `Batch` corresponding to the `proposal`. */
    batch: TAccountMetas[2];
    /** The rent collector. */
    rentCollector: TAccountMetas[3];
    systemProgram: TAccountMetas[4];
  };
  data: BatchAccountsCloseInstructionData;
};

export function parseBatchAccountsCloseInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBatchAccountsCloseInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      multisig: getNextAccount(),
      proposal: getNextAccount(),
      batch: getNextAccount(),
      rentCollector: getNextAccount(),
      systemProgram: getNextAccount(),
    },
    data: getBatchAccountsCloseInstructionDataDecoder().decode(instruction.data),
  };
}
