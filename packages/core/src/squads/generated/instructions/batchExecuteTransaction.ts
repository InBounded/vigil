/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  combineCodec,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type InstructionAccountInput,
  type InstructionAccountInputAddress,
  type InstructionSignerInput,
  type ResolvedInstructionAccount,
  type ResolvedInstructionAccountMeta,
} from "@solana/program-client-core";
import { SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS } from "../programs/index.js";

export const BATCH_EXECUTE_TRANSACTION_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  172, 44, 179, 152, 21, 127, 234, 180,
]);

export function getBatchExecuteTransactionDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(BATCH_EXECUTE_TRANSACTION_DISCRIMINATOR);
}

export type BatchExecuteTransactionInstruction<
  TProgram extends string = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
  TAccountMultisig extends string | AccountMeta<string> = string,
  TAccountMember extends string | AccountMeta<string> = string,
  TAccountProposal extends string | AccountMeta<string> = string,
  TAccountBatch extends string | AccountMeta<string> = string,
  TAccountTransaction extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMultisig extends string ? ReadonlyAccount<TAccountMultisig> : TAccountMultisig,
      TAccountMember extends string
        ? ReadonlySignerAccount<TAccountMember> & AccountSignerMeta<TAccountMember>
        : TAccountMember,
      TAccountProposal extends string ? WritableAccount<TAccountProposal> : TAccountProposal,
      TAccountBatch extends string ? WritableAccount<TAccountBatch> : TAccountBatch,
      TAccountTransaction extends string
        ? ReadonlyAccount<TAccountTransaction>
        : TAccountTransaction,
      ...TRemainingAccounts,
    ]
  >;

export type BatchExecuteTransactionInstructionData = {
  discriminator: ReadonlyUint8Array;
};

export type BatchExecuteTransactionInstructionDataArgs = {};

export function getBatchExecuteTransactionInstructionDataEncoder(): FixedSizeEncoder<BatchExecuteTransactionInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]),
    (value) => ({
      ...value,
      discriminator: BATCH_EXECUTE_TRANSACTION_DISCRIMINATOR,
    }),
  );
}

export function getBatchExecuteTransactionInstructionDataDecoder(): FixedSizeDecoder<BatchExecuteTransactionInstructionData> {
  return getStructDecoder([["discriminator", fixDecoderSize(getBytesDecoder(), 8)]]);
}

export function getBatchExecuteTransactionInstructionDataCodec(): FixedSizeCodec<
  BatchExecuteTransactionInstructionDataArgs,
  BatchExecuteTransactionInstructionData
> {
  return combineCodec(
    getBatchExecuteTransactionInstructionDataEncoder(),
    getBatchExecuteTransactionInstructionDataDecoder(),
  );
}

export type BatchExecuteTransactionInput<
  TAccountMultisig extends InstructionAccountInput = InstructionAccountInput,
  TAccountMember extends InstructionSignerInput = InstructionSignerInput,
  TAccountProposal extends InstructionAccountInput = InstructionAccountInput,
  TAccountBatch extends InstructionAccountInput = InstructionAccountInput,
  TAccountTransaction extends InstructionAccountInput = InstructionAccountInput,
> = {
  /** Multisig account this batch belongs to. */
  multisig: TAccountMultisig;
  /** Member of the multisig. */
  member: TAccountMember;
  /**
   * The proposal account associated with the batch.
   * If `transaction` is the last in the batch, the `proposal` status will be set to `Executed`.
   */
  proposal: TAccountProposal;
  batch: TAccountBatch;
  /** Batch transaction to execute. */
  transaction: TAccountTransaction;
};

export function getBatchExecuteTransactionInstruction<
  TAccountMultisig extends InstructionAccountInput,
  TAccountMember extends InstructionSignerInput,
  TAccountProposal extends InstructionAccountInput,
  TAccountBatch extends InstructionAccountInput,
  TAccountTransaction extends InstructionAccountInput,
  TProgramAddress extends Address = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
>(
  input: BatchExecuteTransactionInput<
    TAccountMultisig,
    TAccountMember,
    TAccountProposal,
    TAccountBatch,
    TAccountTransaction
  >,
  config?: { programAddress?: TProgramAddress },
): BatchExecuteTransactionInstruction<
  TProgramAddress,
  ResolvedInstructionAccountMeta<
    TAccountMultisig,
    InstructionAccountInputAddress<TAccountMultisig>
  >,
  ResolvedInstructionAccountMeta<TAccountMember, InstructionAccountInputAddress<TAccountMember>>,
  ResolvedInstructionAccountMeta<
    TAccountProposal,
    InstructionAccountInputAddress<TAccountProposal>
  >,
  ResolvedInstructionAccountMeta<TAccountBatch, InstructionAccountInputAddress<TAccountBatch>>,
  ResolvedInstructionAccountMeta<
    TAccountTransaction,
    InstructionAccountInputAddress<TAccountTransaction>
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS;

  // Account meta helper.
  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");

  // Original accounts.
  const originalAccounts = {
    multisig: {
      value: input.multisig ?? null,
      isSigner: false,
      isWritable: false,
    },
    member: { value: input.member ?? null, isSigner: true, isWritable: false },
    proposal: {
      value: input.proposal ?? null,
      isSigner: false,
      isWritable: true,
    },
    batch: { value: input.batch ?? null, isSigner: false, isWritable: true },
    transaction: {
      value: input.transaction ?? null,
      isSigner: false,
      isWritable: false,
    },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  return Object.freeze({
    accounts: [
      getAccountMeta("multisig", accounts.multisig),
      getAccountMeta("member", accounts.member),
      getAccountMeta("proposal", accounts.proposal),
      getAccountMeta("batch", accounts.batch),
      getAccountMeta("transaction", accounts.transaction),
    ],
    data: getBatchExecuteTransactionInstructionDataEncoder().encode({}),
    programAddress,
  } as BatchExecuteTransactionInstruction<
    TProgramAddress,
    ResolvedInstructionAccountMeta<
      TAccountMultisig,
      InstructionAccountInputAddress<TAccountMultisig>
    >,
    ResolvedInstructionAccountMeta<TAccountMember, InstructionAccountInputAddress<TAccountMember>>,
    ResolvedInstructionAccountMeta<
      TAccountProposal,
      InstructionAccountInputAddress<TAccountProposal>
    >,
    ResolvedInstructionAccountMeta<TAccountBatch, InstructionAccountInputAddress<TAccountBatch>>,
    ResolvedInstructionAccountMeta<
      TAccountTransaction,
      InstructionAccountInputAddress<TAccountTransaction>
    >
  >);
}

export type ParsedBatchExecuteTransactionInstruction<
  TProgram extends string = typeof SQUADS_MULTISIG_PROGRAM_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    /** Multisig account this batch belongs to. */
    multisig: TAccountMetas[0];
    /** Member of the multisig. */
    member: TAccountMetas[1];
    /**
     * The proposal account associated with the batch.
     * If `transaction` is the last in the batch, the `proposal` status will be set to `Executed`.
     */
    proposal: TAccountMetas[2];
    batch: TAccountMetas[3];
    /** Batch transaction to execute. */
    transaction: TAccountMetas[4];
  };
  data: BatchExecuteTransactionInstructionData;
};

export function parseBatchExecuteTransactionInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedBatchExecuteTransactionInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 5) {
    throw new SolanaError(SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS, {
      actualAccountMetas: instruction.accounts.length,
      expectedAccountMetas: 5,
    });
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      multisig: getNextAccount(),
      member: getNextAccount(),
      proposal: getNextAccount(),
      batch: getNextAccount(),
      transaction: getNextAccount(),
    },
    data: getBatchExecuteTransactionInstructionDataDecoder().decode(instruction.data),
  };
}
